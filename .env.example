# Ver 05_TECH_STACK.md — las API Keys nunca se guardan en texto plano en producción,
# usar almacenamiento seguro del sistema operativo (keyring) cuando sea posible.

# LLM Providers (AI Agnostic — todos opcionales, ver ai/models)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

# Backend local (apps/api)
API_HOST=127.0.0.1
API_PORT=8000

# Base de datos (SQLite en MVP)
DATABASE_URL=sqlite:///./storage/confector.db

# Vector DB
CHROMA_PERSIST_DIR=./storage/chroma
