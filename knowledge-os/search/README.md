# knowledge-os/search

Búsqueda sobre el conocimiento del proyecto (12§7.3).

**Responsabilidad**: keyword search, semantic search, hybrid search sobre `knowledge-os/embeddings` y `knowledge-os/graph`.

**No hace**: no genera respuestas — solo recupera. La generación de respuesta la hace `ai/agents` (Copilot).
