# knowledge-os/retrieval

RAG (Retrieval Augmented Generation) — combina `search`, `graph` y `embeddings` para construir contexto recuperado antes de invocar un modelo IA.

**No hace**: no llama modelos IA directamente (eso es `ai/models`), solo recupera y ensambla contexto.
